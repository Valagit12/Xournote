This is the folder for adding new features to the app. Inside each feature subfolder at this directory, there will be folders that mimic the project's structure. So components, context, data, hooks, etc, but specific to that feature.
index.js will be where you will export the components from and import from for everywhere else
